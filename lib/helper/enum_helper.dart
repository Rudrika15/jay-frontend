enum LeaveType {all, today}
enum TimePeriod {all, today}
enum UserRole {admin, staff, client}
enum TimeSlot {morning, afternoon, evening}
enum CallStatusEnum {pending, cancelled, waiting, allocated, completed}